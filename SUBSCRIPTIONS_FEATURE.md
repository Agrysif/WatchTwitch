# 🎯 Новая Фича: Управление Подписками

## 📋 Описание

Новая вкладка **"Подписки"** добавляет удобный инструмент для управления каналами, на которые вы подписаны на Twitch. Это позволяет легко контролировать и приоритизировать фарминг дропсов на понравившихся вам каналах.

## 🎨 Функции

### 1. **Просмотр подписок**

- Список всех каналов, на которые вы подписаны
- Информация о количестве фолловеров
- Время последнего стрима
- Статус активности канала

### 2. **Система рейтинга**

Каждый канал получает автоматический рейтинг (0-100) на основе:

- **Частота стримов** (30%) - как часто канал стримит
- **Регулярность** (25%) - консистентность трансляций
- **Популярность** (20%) - количество фолловеров
- **Свежесть контента** (15%) - время последнего стрима
- **Наличие дропсов** (10%) - бонус если есть дропсы

**Визуализация рейтинга:**

- ⭐ **Хороший рейтинг** (70+) - зелёный цвет
- → **Средний рейтинг** (40-69) - фиолетовый цвет
- ✗ **Плохой рейтинг** (<40) - красный цвет

### 3. **Фильтры**

- **Все** - показывает все подписки
- **Активные** - каналы, которые стримили в последние 30 дней
- **С дропсами** - только каналы с доступными дропсами
- **Неактивные** - каналы без трансляций более 30 дней
- **⭐ Хороший рейтинг** - каналы с рейтингом 70+

### 4. **Управление**

- 🔄 **Обновить** - получить свежие данные о подписках
- 🗑️ **Очистить неактивные** - отписаться от всех неактивных каналов

### 5. **Приоритет подписок при фарминге**

Включив галочку **"Приоритет подписок при фарминге"**, система будет:

1. Сначала проверять подписанные каналы с дропсами
2. Затем переходить на остальные категории по стандартной логике

### 6. **Быстрые действия**

Для каждого канала доступны:

- 📺 **Открыть на Twitch** - перейти на страницу канала
- 💎 **Начать фарминг** - добавить канал в фарминг и начать смотреть (если есть дропсы)
- ✕ **Отписаться** - отписаться от канала

### 7. **Информационная панель**

- Всего подписок
- Каналов с дропсами
- Активные / неактивные каналы

### 8. **Рекомендации**

- 🟢 **Активен** - канал регулярно стримит
- 🟡 **Редко стримит** - канал стримил давно (более 7 дней)
- 🔴 **Неактивен** - канал не стримил более 30 дней
- ⚠️ **Рекомендуется отписка** - если канал давно не стримит или имеет очень низкий рейтинг

## 🔧 Технические детали

### Новые файлы:

- `renderer/pages/subscriptions.html` - UI страницы
- `renderer/js/pages/subscriptions-page.js` - логика страницы

### Изменённые файлы:

- `renderer/index.html` - добавлена вкладка в меню и подгрузка скриптов
- `renderer/js/router.js` - добавлена маршрутизация на новую страницу
- `renderer/js/storage.js` - методы для сохранения подписок
- `renderer/js/app.js` - добавлена функция `showConfirmation` для подтверждений
- `renderer/js/pages/farming-page.js` - интеграция приоритета подписок в фарминг, метод `addChannelToFarming`
- `preload.js` - expose API для работы с подписками
- `main.js` - handlers для получения подписок и отписки от каналов

### Новые IPC Handlers:

```javascript
// Получение подписок текущего пользователя
ipcMain.handle('get-user-subscriptions', async (event, authToken) => {...})

// Отписка от канала
ipcMain.handle('unsubscribe-channel', async (event, authToken, channelId) => {...})
```

## 📊 Логика интеграции с фармингом

Когда включен приоритет подписок:

```
switchToNextEnabledCategory() {
  if (subscriptionsPriorityEnabled) {
    // Первыми идут подписанные каналы с дропсами
    enabledCategories.sort((a, b) => {
      const aIsSubscribed = isInSubscriptions(a);
      const bIsSubscribed = isInSubscriptions(b);
      if (aIsSubscribed !== bIsSubscribed) return aIsSubscribed ? -1 : 1;
      // Затем стандартная сортировка
      ...
    });
  }
}
```

## 🚀 Как использовать

1. **Откройте вкладку "Подписки"**
   - Перейдите на вкладку "Подписки" в главном меню

2. **Посмотрите свои подписки**
   - Система автоматически загружает список ваших подписок
   - Каждый канал показывает рейтинг и статус активности

3. **Фильтруйте каналы**
   - Используйте фильтры для поиска нужных каналов

4. **Включите приоритет подписок**
   - Включите галочку "Приоритет подписок при фарминге"
   - Теперь при фарминге система будет проверять подписки в первую очередь

5. **Начните фарминг канала**
   - Нажмите кнопку 💎 для быстрого запуска фарминга выбранного канала
   - Канал будет добавлен в список фарминга (если его ещё нет)

6. **Управляйте подписками**
   - Отписывайтесь от неактивных каналов
   - Используйте "Очистить неактивные" для массовой отписки

## 📝 Примеры

### Пример 1: Быстрый фарминг канала

```
1. Откройте вкладку "Подписки"
2. Найдите канал с дропсами (отмечен 💎)
3. Нажмите на кнопку 💎 "Начать фарминг"
4. Автоматически переключитесь на странице фарминга
```

### Пример 2: Фарминг только подписок

```
1. На вкладке "Подписки" включите "Приоритет подписок при фарминге"
2. На странице фарминга добавьте категории обычным способом
3. Когда система переключается между категориями, она сначала проверит подписки
4. Если у подписанного канала есть дропсы - сначала смотрит его
```

### Пример 3: Отписка от неактивных

```
1. Откройте вкладку "Подписки"
2. Установите фильтр "Неактивные"
3. Нажмите "Очистить неактивные"
4. Подтвердите действие
5. Отписались от всех неактивных каналов
```

## 🔐 Безопасность

- Все операции требуют OAuth авторизации
- Данные подписок хранятся локально
- API запросы идут напрямую через GraphQL Twitch API
- Токены не передаются третьим сторонам

## ⚡ Оптимизация

- Подписки кэшируются локально
- При переключении вкладок система не перезагружает данные
- Рейтинги пересчитываются динамически при загрузке
- GraphQL запросы оптимизированы для минимизации трафика

## 🐛 Известные особенности

- Первая загрузка подписок может занять 5-10 секунд
- Рейтинги рассчитываются на основе доступных данных Twitch API
- Статус "неактивный" определяется по времени последнего стрима
- При отписке нужно подтвердить действие

## 📚 Версия

Добавлено в: v1.0.8 (patch)
